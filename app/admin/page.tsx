'use client';
import { useEffect, useState } from 'react';
const options=[600,800,1000,1200,1500];
export default function AdminPage(){const[data,setData]=useState<any>(null);const[msg,setMsg]=useState('');const[err,setErr]=useState('');const[busy,setBusy]=useState(false);async function load(){setErr('');try{const r=await fetch('/api/config?x='+Date.now());const d=await r.json();if(!r.ok||!d.ok)throw new Error(d.error||'Errore');setData(d)}catch(e:any){setErr(e?.message||'Errore caricamento')}}async function setTotal(n:number){setBusy(true);setErr('');setMsg('');try{const r=await fetch('/api/config',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({totalTiles:n})});const d=await r.json();if(!r.ok||!d.ok)throw new Error(d.error||'Errore');setData(d);setMsg(`Obiettivo impostato a ${n} foto/tessere.`)}catch(e:any){setErr(e?.message||'Errore salvataggio')}finally{setBusy(false)}}useEffect(()=>{load()},[]);const pct=data?Math.min(100,Math.round((data.receivedCount/data.totalTiles)*100)):0;return <main className="container"><section className="card"><h1>Admin fotomosaico</h1><p>Per 180 invitati consiglio di partire da <b>600 foto</b>. Puoi aumentare se arrivano tante foto.</p>{data&&<><div className="bigcount">{data.receivedCount} / {data.totalTiles}</div><div className="progressbar"><div style={{width:`${pct}%`}}/></div><p>{data.complete?'Mosaico completo':`Mancano ${data.missing} foto.`}</p></>}<div className="gridBtns">{options.map(n=><button className="btn" disabled={busy} key={n} onClick={()=>setTotal(n)}>{n} foto</button>)}</div><br/><button className="btn secondary" onClick={load}>Aggiorna conteggio</button>{msg&&<><br/><br/><div className="ok">{msg}</div></>}{err&&<><br/><br/><div className="error" style={{display:'block'}}>{err}</div></>}</section></main>}
